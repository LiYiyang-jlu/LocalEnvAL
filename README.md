# LocalEnvAL
## An atom local environment analysis tool

# Usage
- 导入方法：在根目录下`import LocalEnvAL`


# Something Need to Know：
- 为了适应在jupyter note中使用可交互视图，rdf内部可视化模块依赖`plotly`而非`matplotlib`,但是`plotly`的tex渲染在jupyter中存在一些暂时无法使用的[问题](https://github.com/plotly/plotly.py/issues/4336)